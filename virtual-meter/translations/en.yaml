configuration:
  mock_mode:
    name: Mock Mode
    description: Enable API test mode. Serves static Shelly Gen2 JSON and logs all requests.
  debug_logging:
    name: Debug Logging
    description: Enable verbose debug logs.
  provider_endpoint:
    name: Provider Endpoint
    description: HTTP endpoint to poll for source data.
  poll_interval_ms:
    name: Polling Interval (ms)
    description: Provider endpoint polling interval in milliseconds (250-5000).
  l1_current_json:
    name: L1 Current (JSON Path)
    description: Path to L1 current in provider endpoint, JSON values override numeric values.
  l1_current_value:
    name: L1 Current (A)
    description: Optional numeric override for L1 current. Used if JSON path is not set or empty.
  l1_voltage_json:
    name: L1 Voltage (JSON Path)
    description: JSON path to L1 voltage in provider endpoint, JSON values override numeric values.
  l1_voltage_value:
    name: L1 Voltage (V)
    description: Optional numeric override for L1 voltage. Used if JSON path is not set or empty.
  l1_act_power_json:
    name: L1 Active Power (JSON Path)
    description: JSON path to L1 active power in provider endpoint, JSON values override numeric values.
  l1_act_power_value:
    name: L1 Active Power (W)
    description: Optional numeric override for L1 active power. Used if JSON path is not set or empty.
  l1_aprt_power_json:
    name: L1 Apparent Power (JSON Path)
    description: JSON path to L1 apparent power in provider endpoint, JSON values override numeric values.
  l1_aprt_power_value:
    name: L1 Apparent Power (VA)
    description: Optional numeric override for L1 apparent power. Used if JSON path is not set or empty.
  l1_pf_json:
    name: L1 Power Factor (JSON Path)
    description: JSON path to L1 power factor in provider endpoint, JSON values override numeric values.
  l1_pf_value:
    name: L1 Power Factor
    description: Optional numeric override for L1 power factor. Used if JSON path is not set or empty.
  l1_freq_json:
    name: L1 Frequency (JSON Path)
    description: JSON path to L1 frequency in provider endpoint, JSON values override numeric values.
  l1_freq_value:
    name: L1 Frequency (value override, optional)
    description: Optional numeric override for L1 frequency. Used if JSON path is not set or empty.
  l2_current_json:
    name: L2 Current (JSON Path)
    description: Path to L2 current in provider endpoint, JSON values override numeric values.
  l2_current_value:
    name: L2 Current (A)
    description: Optional numeric override for L2 current. Used if JSON path is not set or empty.
  l2_voltage_json:
    name: L2 Voltage (JSON Path)
    description: Path to L2 voltage in provider endpoint, JSON values override numeric values.
  l2_voltage_value:
    name: L2 Voltage (V)
    description: Optional numeric override for L2 voltage. Used if JSON path is not set or empty.
  l2_act_power_json:
    name: L2 Active Power (JSON Path)
    description: Path to L2 active power in provider endpoint, JSON values override numeric values.
  l2_act_power_value:
    name: L2 Active Power (W)
    description: Optional numeric override for L2 active power. Used if JSON path is not set or empty.
  l2_aprt_power_json:
    name: L2 Apparent Power (JSON Path)
    description: Path to L2 apparent power in provider endpoint, JSON values override numeric values.
  l2_aprt_power_value:
    name: L2 Apparent Power (VA)
    description: Optional numeric override for L2 apparent power. Used if JSON path is not set or empty.
  l2_pf_json:
    name: L2 Power Factor (JSON Path)
    description: Path to L2 power factor in provider endpoint, JSON values override numeric values.
  l2_pf_value:
    name: L2 Power Factor
    description: Optional numeric override for L2 power factor. Used if JSON path is not set or empty.
  l2_freq_json:
    name: L2 Frequency (JSON Path)
    description: Path to L2 frequency in provider endpoint, JSON values override numeric values.
  l2_freq_value:
    name: L2 Frequency (value override, optional)
    description: Optional numeric override for L2 frequency. Used if JSON path is not set or empty.
  l3_current_json:
    name: L3 Current (JSON Path)
    description: Path to L3 current in provider endpoint, JSON values override numeric values.
  l3_current_value:
    name: L3 Current (A)
    description: Optional numeric override for L3 current. Used if JSON path is not set or empty.
  l3_voltage_json:
    name: L3 Voltage (JSON Path)
    description: Path to L3 voltage in provider endpoint, JSON values override numeric values.
  l3_voltage_value:
    name: L3 Voltage (V)
    description: Optional numeric override for L3 voltage. Used if JSON path is not set or empty.
  l3_act_power_json:
    name: L3 Active Power (JSON Path)
    description: Path to L3 active power in provider endpoint, JSON values override numeric values.
  l3_act_power_value:
    name: L3 Active Power (W)
    description: Optional numeric override for L3 active power. Used if JSON path is not set or empty.
  l3_aprt_power_json:
    name: L3 Apparent Power (JSON Path)
    description: Path to L3 apparent power in provider endpoint, JSON values override numeric values.
  l3_aprt_power_value:
    name: L3 Apparent Power (VA)
    description: Optional numeric override for L3 apparent power. Used if JSON path is not set or empty.
  l3_pf_json:
    name: L3 Power Factor (JSON Path)
    description: Path to L3 power factor in provider endpoint, JSON values override numeric values.
  l3_pf_value:
    name: L3 Power Factor
    description: Optional numeric override for L3 power factor. Used if JSON path is not set or empty.
  l3_freq_json:
    name: L3 Frequency (JSON Path)
    description: Path to L3 frequency in provider endpoint, JSON values override numeric values.
  l3_freq_value:
    name: L3 Frequency (value override, optional)
    description: Optional numeric override for L3 frequency. Used if JSON path is not set or empty.
  total_current_json:
    name: Total Current (JSON Path)
    description: Path to total current in provider endpoint, JSON values override numeric values.
  total_current_value:
    name: Total Current (A)
    description: Optional numeric override for total current. Used if JSON path is not set or empty.
  total_act_power_json:
    name: Total Act Power (JSON Path)
    description: Path to total active power in provider endpoint, JSON values override numeric values.
  total_act_power_value:
    name: Total Act Power (W)
    description: Optional numeric override for total active power. Used if JSON path is not set or empty.
  total_aprt_power_json:
    name: Total Aprt Power (JSON Path)
    description: Path to total apparent power in provider endpoint, JSON values override numeric values.
  total_aprt_power_value:
    name: Total Aprt Power (VA)
    description: Optional numeric override for total apparent power. Used if JSON path is not set or empty.
